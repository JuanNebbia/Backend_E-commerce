<div class="ticket-container">
    <div class="ticket-card">
        <h1 class="ticket-title">COMPROBANTE DE COMPRA</h1>
        <p class="ticket-purchaser">de: {{ticket.purchaser}}</p>
        {{!-- {{#each ticket.products}}
        <div class="product-item-ticket">
            <p>{{product.title}}</p>
            <p>{{quantity}}</p>
            <p>${{product.price}}</p>
        </div>
        {{/each}} --}}
        <table class="ticket-table">
        <tr>
            <th class="header">Producto</th>
            <th class="header">Cantidad</th>
            <th class="header">Precio</th>
        </tr>
        {{#each ticket.products}}
        <tr>
            <td>{{product.title}}</td>
            <td>{{quantity}}</td>
            <td>${{product.price}}</td>
        </tr>
        {{/each}}
    </table>
        <p class="ticket-amount">Total: ${{ticket.amount}}</p>
        <p class="ticket-date">{{ticket.date}}</p>
        <p class="ticket-code">código de compra: {{ticket.code}}</p>
    </div>
</div>